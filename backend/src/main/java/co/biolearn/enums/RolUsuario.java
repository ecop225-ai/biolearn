package co.biolearn.enums;

/**
 * Roles del sistema. Coincide exactamente con el ENUM `tipo_usuario` de la tabla `usuario`.
 */
public enum RolUsuario {
    admin,
    docente,
    estudiante,
    supervisor,
    contador
}
